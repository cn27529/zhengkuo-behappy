#  在 Windows CMD 中成功編譯

[target.x86_64-pc-windows-gnu]
linker = "gcc"
ar = "ar"

[env]
# 使用相對路徑，讓系統在 PATH 中查找
CC = "gcc"
TARGET_CC = "gcc"
TARGET_AR = "ar"
CC_x86_64-pc-windows-gnu = "gcc"
AR_x86_64-pc-windows-gnu = "ar"

# 這個很重要 - 避免 MSYS2 路徑轉換
MSYS2_PATH_TYPE = "inherit"
