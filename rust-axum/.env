# 服務器配置
HOST=127.0.0.1
PORT=3000

# SQLite 數據庫配置
# 1: 使用相對路徑 (推薦開發環境)
DATABASE_URL=sqlite:../db/data.db

# 2: 使用絕對路徑
# DATABASE_URL=sqlite:/absolute/path/to/your/database.db

# 3: 使用內存數據庫 (測試用，重啟後數據消失)
# DATABASE_URL=sqlite::memory:

# 4. 生產環境建議使用 PostgreSQL
#DATABASE_URL=postgresql://user:pass@host:5432/dbname

# JWT 配置
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=86400  # 24小時，單位：秒

# CORS 配置
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# 日誌級別
RUST_LOG=debug

# SQLite 特定配置 (可選)
# SQLite 連接池大小
DATABASE_MAX_CONNECTIONS=5

# SQLite 性能優化
SQLITE_JOURNAL_MODE=WAL  # Write-Ahead Logging，提升並發性能
SQLITE_SYNCHRONOUS=NORMAL  # 平衡安全性和性能
