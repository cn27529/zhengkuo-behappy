{
  "name": "backend",
  "version": "1.0.0",
  "description": "client build cp to backend",
  "main": "server.js",
  "scripts": {
    "build": "cd ../client && rm -rf dist && vite build",
    "start": "node server.js",
    "copy": "rm -rf dist && cp -r ../client/dist/. dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "rollup-plugin-analyzer": "^4.0.0",
    "rollup-plugin-visualizer": "^6.0.5",
    "vite": "^4.4.5",
    "vite-bundle-analyzer": "^1.3.2",
    "vite-plugin-compression": "^0.5.1"
  }
}